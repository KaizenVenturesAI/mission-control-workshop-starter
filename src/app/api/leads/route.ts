/**
 * @deprecated Legacy route — use /api/inbound instead.
 * Kept for backward compatibility only. Do not add new functionality here.
 */
export const dynamic = "force-dynamic";

export { GET, POST } from "@/app/api/inbound/route";
